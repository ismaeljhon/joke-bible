import './site-header.scss'

const SiteHeader = () => {
  return (
    <header className="AppHeader">
      <nav>
        <a>So Funktioniert's</a>
        <a>Sonderangebote</a>
        <a>Mein Bereich</a>
      </nav>
    </header>
  );
}

export default SiteHeader;