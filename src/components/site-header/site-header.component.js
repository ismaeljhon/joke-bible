const SiteHeader = () => {
  return (
    <header className="App-header">
      <nav>
        <a>So Funktioniert's</a>
        <a>Sonderangebote</a>
        <a>Mein Bereich</a>
      </nav>
    </header>
  );
}

export default SiteHeader;